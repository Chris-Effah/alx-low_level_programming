#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: a - first integer to be multiplied
 * @b: b - second integer to be multiplied
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
