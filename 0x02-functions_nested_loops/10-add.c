#include "main.h"
/**
 * add -  a function that adds two integers and returns the result.
 * @x: First input
 * @y: Second input
 * Return: The result of the sum of two integers
 */
int add(int x, int y)
{
	return (x + y);
}
